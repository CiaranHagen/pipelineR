# This file is part of the testthat test infrastructure.
# Run `devtools::test()` or `testthat::test_check("pipelineR")` to execute all tests.

library(testthat)
library(pipelineR)

test_check("pipelineR")
